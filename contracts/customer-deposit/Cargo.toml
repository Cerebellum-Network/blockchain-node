[package]
name = "customer-deposit"
version = "5.1.0"
authors = ["Use Ink <ink@use.ink>"]
edition = "2021"
publish = false

[dependencies]
base64ct = { version = "=1.6.0" }
ddc-primitives = { path = "/Users/yahortsaryk/work/workspace-blockchain/ddc-primitives", default-features = false }
ink = { version = "5.1.0", default-features = false }

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = [
  "ink/std",
  "ddc-primitives/std",
]

ink-as-dependency = []
