//! Stub for pallet_hyper_fungible_token weights.
//!
//! We use `type WeightInfo = ();` in the runtime Config for now, so this
//! module exists only to keep the mod tree intact and to be a target for the
//! Generate Weights workflow to overwrite once the benchmark suite is wired up.

#![allow(unused)]
